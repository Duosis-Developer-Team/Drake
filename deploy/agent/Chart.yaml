apiVersion: v2
name: drake-cluster-agent
description: >-
  Drake read-only cluster agent. Outbound-only mTLS to the Drake control
  plane; exact get/list/watch RBAC; no Secret or ConfigMap access; no exec,
  attach, or portforward; single replica by design (crash-safe, no Lease).
type: application
version: 0.4.0
appVersion: "0.4.0"
