# Must be rejected: private key material embedded in a manifest value.
# The block below is a DELIBERATELY FAKE, non-functional placeholder.
apiVersion: drake.duosis.com/v1alpha1
kind: ProjectObservability
metadata: {name: alpha}
spec:
  repository: {provider: github, owner: example-org, name: alpha}
  owners: [{team: platform}]
  environments:
    - {name: dev, runtime: kubernetes, branch: dev, clusterRef: cluster-a, namespace: alpha-dev, criticality: medium}
  services:
    - name: api
      component: api
      runtime: fastapi
      metricsProfile: fastapi-v1
      workloadSelector:
        material: "-----BEGIN PRIVATE KEY-----FAKEFIXTUREDATA-----END PRIVATE KEY-----"
  tenantModel: {mode: none}
