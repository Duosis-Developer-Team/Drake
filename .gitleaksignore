# Exact single-finding exemptions ONLY (no path or shape allowlists).
# Each entry below is a Sprint 3 telemetry-registry identifier (dotted
# .vN template keys / hyphenated widget keys in the registry JSON files)
# that the generic-api-key rule flags on entropy.
# Reviewed as registry identifiers, not credentials. Two runtime canaries
# (scripts/secret-scan.sh) plant credential-shaped values in exactly these
# JSON field shapes and MUST be detected — proving these fingerprints do
# not blind the scanner to real secrets in the same fields.
#
# Tree findings (line-scoped, so they MOVE when the registry grows).
# Re-derived again in Sprint 13F.6: the environment workload templates
# were inserted and the template list is kept sorted, so three of the six
# lines moved. The findings themselves are unchanged registry identifiers. Each one is a registry identifier —
# `latency-p95-kpi`, `environment.scrape-up.v1` and friends — flagged by
# the generic rule on entropy, verified by reading the line before it was
# listed here. The canaries below still plant credential-shaped values in
# these same JSON fields and must still be caught.
/repo/.secret-scan-tree/packages/contracts/registry/dashboard-templates.json:generic-api-key:103
/repo/.secret-scan-tree/packages/contracts/registry/dashboard-templates.json:generic-api-key:113
/repo/.secret-scan-tree/packages/contracts/registry/dashboard-templates.json:generic-api-key:171
/repo/.secret-scan-tree/packages/contracts/registry/dashboard-templates.json:generic-api-key:174
/repo/.secret-scan-tree/packages/contracts/registry/dashboard-templates.json:generic-api-key:189
/repo/.secret-scan-tree/packages/contracts/registry/dashboard-templates.json:generic-api-key:296
/repo/.secret-scan-tree/packages/contracts/registry/query-templates.json:generic-api-key:322
/repo/.secret-scan-tree/packages/contracts/registry/query-templates.json:generic-api-key:443
/repo/.secret-scan-tree/packages/contracts/registry/query-templates.json:generic-api-key:527
/repo/.secret-scan-tree/packages/contracts/registry/query-templates.json:generic-api-key:570

# History findings (commit-scoped):
b3958083c566868468fbeb7ad9f088913283cf80:packages/contracts/registry/dashboard-templates.json:generic-api-key:49
b3958083c566868468fbeb7ad9f088913283cf80:packages/contracts/registry/dashboard-templates.json:generic-api-key:52
b3958083c566868468fbeb7ad9f088913283cf80:packages/contracts/registry/dashboard-templates.json:generic-api-key:67
b3958083c566868468fbeb7ad9f088913283cf80:packages/contracts/registry/dashboard-templates.json:generic-api-key:138
b3958083c566868468fbeb7ad9f088913283cf80:packages/contracts/registry/query-templates.json:generic-api-key:49
b3958083c566868468fbeb7ad9f088913283cf80:packages/contracts/registry/query-templates.json:generic-api-key:137
# Tracked-tree scan findings (same six lines; the tree scan copies the
# tracked file set to /repo/.secret-scan-tree inside the scanner
# container, so dir-mode fingerprints carry that absolute prefix):

# Sprint 5 — the `hermes.pilot.v1` metric preset identifier, flagged by the
# generic-api-key rule on entropy alone. It is a preset contract id: it is
# published by GET /v1/service-health/presets, typed by operators into a
# binding, and authenticates nothing.
#
# The tracked tree needs no entry here — each occurrence carries an inline
# `gitleaks:allow` marker with its justification, so the exemption travels
# with the literal and cannot drift onto another line. These three entries
# cover only the historical commit that introduced the lines before those
# markers existed. A commit's patch is immutable and this branch does not
# rewrite history, so they can never go stale or widen.
cc408f0e7b11531c36c877b6c9988888faa700e9:apps/api/src/drake_api/service_health/presets.py:generic-api-key:113
cc408f0e7b11531c36c877b6c9988888faa700e9:docs/runbooks/SERVICE_HEALTH_BINDINGS.md:generic-api-key:26
cc408f0e7b11531c36c877b6c9988888faa700e9:docs/runbooks/SERVICE_HEALTH_BINDINGS.md:generic-api-key:121

# Sprint 5A CTO fix gate — one commit-scoped exemption.
# 56083ff introduced a test that proves a credential-validation failure
# never quotes the file it read. The assertion needed a PEM-shaped string,
# and that literal is what the private-key rule flagged. It is a test
# fixture, not a key: there is no key body, and no such file is ever read
# outside the test's own tmp_path.
# The literal has since been removed from the tree — the test now builds
# the marker at runtime by concatenation, so the tracked tree is clean and
# no new commit can reintroduce it. This fingerprint covers only the
# historical commit, which is not rewritten because this branch does not
# rewrite history.
56083ff842e5b15af43820e94300e74a76f300a9:apps/api/tests/test_github_contract_unit.py:private-key:105

# Sprint 13F.6 — the failure-signal registry commit.
# Adding "Not running" widgets and their templates put four more registry
# identifiers on lines the generic-api-key rule flags on entropy:
# widget keys like `env-waiting-reason` and template keys like
# `environment.replicas-unavailable.v1`. Reviewed as identifiers, not
# credentials. History fingerprints are commit-scoped and therefore stable;
# only the tree ones above move as the registry grows.
591bd81fde3b63713da4686ec3da9c98626c8512:packages/contracts/registry/dashboard-templates.json:generic-api-key:103
591bd81fde3b63713da4686ec3da9c98626c8512:packages/contracts/registry/dashboard-templates.json:generic-api-key:113
591bd81fde3b63713da4686ec3da9c98626c8512:packages/contracts/registry/query-templates.json:generic-api-key:443
591bd81fde3b63713da4686ec3da9c98626c8512:packages/contracts/registry/query-templates.json:generic-api-key:570
