/**
 * Strings for the `commandCenter` area. English above, Turkish below, same shape —
 * a key missing from `tr` fails typecheck. See ../README.md.
 */
import { defineMessages } from "../define";

export const commandCenter = defineMessages({
  en: {},
  tr: {},
});
