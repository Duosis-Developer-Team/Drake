/**
 * Strings for the `catalog` area. English above, Turkish below, same shape —
 * a key missing from `tr` fails typecheck. See ../README.md.
 */
import { defineMessages } from "../define";

export const catalog = defineMessages({
  en: {},
  tr: {},
});
