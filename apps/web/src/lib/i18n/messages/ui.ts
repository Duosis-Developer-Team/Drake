/**
 * Strings for the `ui` area. English above, Turkish below, same shape —
 * a key missing from `tr` fails typecheck. See ../README.md.
 */
import { defineMessages } from "../define";

export const ui = defineMessages({
  en: {},
  tr: {},
});
