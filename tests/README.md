# Cross-cutting tests

This directory hosts test assets that span multiple workspaces:

- `contract/` — cross-service contract fixtures (populated from Sprint 1 on).
- `e2e/` — end-to-end scenarios (populated from Sprint 2 on).

Unit and integration tests live inside each workspace
(`apps/*/tests`, `packages/*/src/**/*.test.ts`).
